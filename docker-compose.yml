services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${WEB_PORT:-5000}:5000"
    volumes:
      - .:/app
      - db_data:/data

volumes:
  db_data:
